// Main module declarations for the project
mod contracts {
    mod escrow_src;
    mod htlc_validator;
    mod escrow_factory;
}

mod interfaces {
    mod ierc20;
}

mod utils {
    mod hash_utils;
    mod timestamp;
}